# Ralph Progress Log
Started: Sun Mar 22 15:42:51 UTC 2026

## Codebase Patterns
- Server typechecks with `npx tsc --noEmit -p tsconfig.server.json` (separate from frontend tsconfig)
- Frontend typechecks with `npx tsc --noEmit` (uses tsconfig.json, includes `src/` only)
- Frontend API hooks pattern: `src/api/<resource>.ts` with interfaces + useQuery/useMutation hooks, using `apiFetch` from `src/api/client.ts`
- Query keys follow `["resource", id]` pattern — e.g. `["pipelines", projectId]`, `["pipelineStages", pipelineId]`
- Use `enabled: !!param` in useQuery to skip fetching when the param is null
- `useProjectsStore((s) => s.activeProjectId)` gives the currently selected project ID
- Pipeline stages from API have `agentId` (not agent name) and `description` (not `note`)
- Server code uses `.js` extensions in imports despite being `.ts` files (ESM convention)
- Server runs via `tsx` — no compilation step needed
- DB schema uses drizzle-orm with SQLite (better-sqlite3)
- Existing agent DB schema in `server/db/schema/agents.ts` has status enum: idle, working, paused, error
- Pipeline stages have status enum: queued, running, done, needs-you, error, skipped
- WebSocket broadcast utility in `server/ws/handler.ts` — use `broadcast(event, data)`
- Agent adapters use lazy process init — constructor takes config, process spawns on first `sendMessage()`
- Agent adapters use a pending message queue + resolver pattern for async response consumption
- MCP adapter uses JSON-RPC 2.0 protocol — requests have `id` (get response), notifications have no `id`
- MCP handshake: initialize → initialized notification → ready for tool calls
- Factory `createAgent()` is async — returns `Promise<AgentAdapter>` because 'auto' mode probes MCP
- Settings table is workspace-scoped: query with `and(eq(workspaceId), eq(key))` pattern
- Use `getTransportSetting(workspaceId)` to read transport preference from DB, defaults to 'auto'
- PipelineEngine: instantiate with `new PipelineEngine(pipelineId)`, then call `await engine.run()`
- Use `asc()` / `desc()` from `drizzle-orm` for orderBy clauses
- Use `typeof table.$inferSelect` to get the row type from a drizzle schema table
- Adding enum values to drizzle SQLite text columns only changes TS types — no DB migration needed
- Parallel stages: `groupStagesBySortOrder()` groups by sortOrder, `executeStageGroup()` runs groups via `Promise.allSettled`
- Checkpointing is at group level (in `executeStageGroup`), not individual stage level
- PipelineEngine `pause()` is sync (sets flag), `cancel()` is async (aborts agents) — callers need to match
- Pipeline status enum: idle, running, completed, error, paused, cancelled
- PipelineEngine instances are stateful — keep a reference to call `pause()`/`cancel()` on a running pipeline
- Fastify route plugin pattern: export async function, register with `server.register()`, use schema objects for validation
- Running engines tracked in `runningEngines` Map in `server/routes/pipelines.ts` — needed for pause/cancel control
- Fire-and-forget async with `.finally()` to clean up engine references: `engine.run().finally(() => runningEngines.delete(id))`
- Use `and()` from `drizzle-orm` for compound WHERE clauses: `and(eq(col1, val1), eq(col2, val2))`
- Stage sub-resource routes live in `server/routes/pipelines.ts` alongside pipeline routes (no separate file)
- Use `isNull()` from `drizzle-orm` for NULL checks in WHERE clauses
- Decision "pending" state is `resolution IS NULL` — no separate status column
- WebSocket events for decisions: `decision:created`, `decision:resolved` — broadcast via `broadcast()` from `server/ws/handler.ts`
- Frontend mutation hooks: `useMutation` with `mutationFn` + `onSuccess` that invalidates related query keys via `useQueryClient`
- Button enable/disable: use `isPending` from `useMutation` to prevent double-click during in-flight requests
- Decision query key: `["decisions", projectId]` — invalidate after resolving a decision
- When a component needs its own mutation state (e.g., each decision card), extract it as a separate component with its own hooks
- Steer endpoint pattern: route-over-existing-ops, no new engine methods needed — parse keyword, delegate to DB/engine
- WebSocket hook pattern: `useWebSocket()` in App.tsx — listens for server events, invalidates TanStack Query caches via partial `queryKey` matches
- WebSocket events from server: `pipeline:status`, `stage:status`, `decision:created`, `decision:resolved`
- Query invalidation with partial keys (e.g., `{ queryKey: ["pipelines"] }`) invalidates all queries whose key starts with that prefix

---

## 2026-03-22 - US-001
- Created `server/agents/types.ts` with AgentAdapter interface, AgentMessage, AgentStatus, AgentConfig types
- Created `server/agents/index.ts` as barrel file re-exporting all types
- Files changed: server/agents/types.ts, server/agents/index.ts
- **Learnings for future iterations:**
  - AgentStatus type uses 'disconnected' while DB agent schema uses 'paused' — these are different enums for different purposes (runtime adapter vs persisted state)
  - Transport-agnostic interface: all adapters must implement sendMessage, getResponse, abort, and expose status
  - Frontend tsconfig only covers `src/` — pre-existing type errors there don't affect server work
---

## 2026-03-22 - US-002
- Created `server/agents/stdio-adapter.ts` implementing AgentAdapter interface
- Spawns `claude` as child process with `--dangerously-skip-permissions` flag
- JSON lines protocol over stdin/stdout with line-buffered parsing
- Handles process exit, error events, and abort (SIGTERM)
- Exported `StdioAdapter` from barrel file `server/agents/index.ts`
- Files changed: server/agents/stdio-adapter.ts, server/agents/index.ts
- **Learnings for future iterations:**
  - Use `node:child_process` spawn with `stdio: ["pipe", "pipe", "pipe"]` for full control
  - JSON line parsing needs a buffer to handle partial reads — split on `\n`, keep last fragment
  - Non-JSON stdout lines are wrapped as plain text messages (graceful degradation)
  - Pending message queue + resolver pattern allows both sync and async consumption of responses
  - StdioAdapter constructor takes AgentConfig but does NOT spawn the process until first `sendMessage()` call (lazy init)
---

## 2026-03-22 - US-003
- Created `server/agents/mcp-adapter.ts` implementing AgentAdapter via MCP protocol (JSON-RPC 2.0)
- Spawns `claude --mcp-server` as child process, communicates via JSON-RPC over stdin/stdout
- MCP handshake: initialize request → initialized notification → ready
- tool_call messages sent as `tools/call` RPC, responses mapped to AgentMessage tool_result
- Handles connection errors with exponential backoff reconnection (max 3 attempts)
- Exported `McpAdapter` from barrel file `server/agents/index.ts`
- Files changed: server/agents/mcp-adapter.ts, server/agents/index.ts
- **Learnings for future iterations:**
  - MCP protocol uses JSON-RPC 2.0: requests have `id` field, notifications don't
  - McpAdapter uses `ensureConnection()` which spawns + handshakes (lazy init like StdioAdapter)
  - pendingRpcs map tracks in-flight JSON-RPC requests by id, separate from the AgentMessage queue
  - Reconnection uses exponential backoff: 1s, 2s, 4s, 8s max with 3 retries
  - Server-initiated notifications (no `id`) are mapped to AgentMessage type 'message'
---

## 2026-03-22 - US-004
- Created `server/agents/factory.ts` with `createAgent()` and `getTransportSetting()` functions
- Factory routes 'stdio' → StdioAdapter, 'mcp' → McpAdapter, 'auto' → probe MCP then fallback to stdio
- `getTransportSetting(workspaceId)` reads `claude_code.transport` key from settings table, defaults to 'auto'
- MCP probe spawns short-lived `claude --mcp-server` process with 2s timeout to check availability
- Exported `createAgent` and `getTransportSetting` from barrel file `server/agents/index.ts`
- Files changed: server/agents/factory.ts, server/agents/index.ts
- **Learnings for future iterations:**
  - Settings table uses key/value pattern with JSON value column — query with `eq(key, "dotted.key.name")`
  - MCP probe needs a settle guard to prevent double-resolve (process can emit both error and exit)
  - `createAgent` is async because 'auto' mode requires probing — callers must await
  - For 'auto', probe is done at creation time, not deferred to first sendMessage — keeps the adapter interface simple
---

## 2026-03-22 - US-005
- Created `server/pipeline/engine.ts` with PipelineEngine class
- Constructor takes pipeline ID, `load()` fetches pipeline and stages from DB
- `run()` executes stages sequentially in sortOrder (ascending)
- Each stage: sets status to 'running', builds AgentConfig from agents table + stage overrides, spawns agent via factory, sends prompt, waits for response, sets status to 'done' or 'error'
- Pipeline status transitions: idle → running → completed (or error)
- Broadcasts WebSocket events: `pipeline:status` and `stage:status`
- Agent cleanup in `finally` block to prevent orphaned processes
- Files changed: server/pipeline/engine.ts
- **Learnings for future iterations:**
  - `asc()` from drizzle-orm is used for orderBy — import `{ asc }` from `"drizzle-orm"`
  - Stage's `agentId` is optional — build a default AgentConfig from stage name/model when no agent record exists
  - Pipeline's `workspaceId` is needed to resolve transport setting — always load the pipeline row first
  - Stage `description` is used as the agent prompt, falling back to `name` if description is null
  - PipelineEngine is a stateful class — instantiate per-pipeline, not a singleton
  - After `getResponse()`, always `abort()` the agent in a finally block to clean up child processes
---

## 2026-03-22 - US-006
- Added checkpoint tracking: `updateCheckpoint(stageId)` called after each successful stage, persists `checkpointStageId` to pipelines table
- Added `resume()` method that finds the stage after `checkpointStageId` and skips stages with status 'done'
- Added 'paused' to pipeline status enum in `server/db/schema/pipelines.ts`
- Updated `updatePipelineStatus()` type to accept 'paused'
- Files changed: server/pipeline/engine.ts, server/db/schema/pipelines.ts
- **Learnings for future iterations:**
  - Pipeline schema already had `checkpointStageId` column — just needed to write to it
  - `getResumeIndex()` uses `findIndex` on sorted stages array — returns index after checkpoint, or 0 if no checkpoint
  - `resume()` both skips via index (start after checkpoint) AND checks `stage.status === 'done'` for safety
  - When adding new enum values to drizzle schema, it only affects TypeScript types — no migration needed for SQLite text columns
---

## 2026-03-22 - US-007
- Added `groupStagesBySortOrder()` to group stages by their `sortOrder` value into arrays
- Added `executeStageGroup()` that runs single stages directly or multiple stages in parallel via `Promise.allSettled`
- Parallel stages all complete even if some fail; partial failure stops the pipeline but doesn't kill running siblings
- Moved checkpoint logic from `executeStage()` to `executeStageGroup()` — checkpoint only after full group success
- Updated `run()` and `resume()` to iterate over groups instead of individual stages
- `resume()` now slices remaining stages, re-groups them, and filters out 'done' stages per group
- Files changed: server/pipeline/engine.ts
- **Learnings for future iterations:**
  - `Promise.allSettled` is key for parallel stages — unlike `Promise.all`, it lets all promises settle before reporting
  - Checkpoint moved to group level: individual `executeStage()` no longer checkpoints, `executeStageGroup()` does
  - For single-stage groups, the fast path avoids `Promise.allSettled` overhead
  - Resume with parallel groups: need to re-group remaining stages and filter 'done' within each group, not just skip by index
  - `Map` preserves insertion order in JS, but we sort explicitly anyway for clarity
---

## 2026-03-22 - US-008
- Added `pause()` method: sets `pauseRequested` flag, updates pipeline status to 'paused', execution loop breaks after current group completes
- Added `cancel()` method: sets `cancelRequested` flag, updates status to 'cancelled', aborts all active agents immediately
- Added `shouldStop()` private helper checked between stage groups in `run()` and `resume()`
- Tracks active agents in `activeAgents` array — populated in `executeStage()`, cleaned up in finally block
- Added 'cancelled' to pipeline status enum in `server/db/schema/pipelines.ts`
- `resume()` now resets interrupt flags before executing
- All state changes persisted to DB and broadcast via WebSocket
- Files changed: server/pipeline/engine.ts, server/db/schema/pipelines.ts
- **Learnings for future iterations:**
  - `pause()` is sync (just sets a flag + DB update), `cancel()` is async (needs to await agent aborts)
  - Cancel takes precedence over pause — if cancel is set, pause is ignored
  - `resume()` must reset both flags so a previously paused/cancelled pipeline can run again
  - Active agent tracking uses array push/filter rather than Set because AgentAdapter doesn't need to be hashable
  - Error handler checks `cancelRequested` to avoid overwriting 'cancelled' status with 'error'
---

## 2026-03-22 - US-009
- Created `server/routes/pipelines.ts` as a Fastify plugin with all pipeline API endpoints
- Endpoints: GET /api/pipelines?projectId, POST /api/pipelines (create with stages), POST /:id/run, /:id/pause, /:id/resume, /:id/cancel
- Registered routes in `server/index.ts` via `server.register(pipelineRoutes)`
- `runningEngines` Map tracks active PipelineEngine instances for pause/cancel control
- Run and resume use fire-and-forget pattern with `.finally()` cleanup
- Input validation via Fastify JSON schema on all POST bodies
- Files changed: server/routes/pipelines.ts (new), server/index.ts
- **Learnings for future iterations:**
  - Fastify route plugins: export async function taking `FastifyInstance`, register with `server.register()`
  - JSON schema validation goes in `schema.body` / `schema.querystring` option object
  - For async background tasks (pipeline execution), fire-and-forget with `engine.run().finally()` — don't await in the handler
  - Running engines need a module-level Map registry — PipelineEngine is stateful and the same instance must be used for pause/cancel
  - Resume creates a new PipelineEngine instance (old one is gone after pause) — engine reloads state from DB
  - Guard against double-run with status check: return 409 if already running
---

## 2026-03-22 - US-010
- Added pipeline stages CRUD endpoints to `server/routes/pipelines.ts`
- Endpoints: GET /:id/stages, POST /:id/stages, PUT /:pipelineId/stages/:stageId, DELETE /:pipelineId/stages/:stageId, POST /:pipelineId/stages/:stageId/skip
- Added `skipped` to stage status enum in schema and engine
- Updated engine `resume()` to also skip stages with status `skipped`
- Input validation on all POST/PUT bodies via Fastify JSON schema
- Files changed: server/routes/pipelines.ts, server/db/schema/pipeline-stages.ts, server/pipeline/engine.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Stage endpoints use `and()` from drizzle-orm for compound WHERE (pipelineId + stageId) to prevent cross-pipeline access
  - When adding a new status value, update both the schema enum AND the engine's `updateStageStatus` type union
  - Default sortOrder for new stages: query existing stages and use max + 1
  - DELETE returns 204 with no body — Fastify handles `return undefined` correctly for this
  - The `skipped` status is set via a dedicated endpoint, not as a general status update — keeps the API intent-driven
---

## 2026-03-22 - US-011
- Created `server/routes/decisions.ts` as a Fastify plugin with decision queue API endpoints
- Endpoints: GET /api/decisions?projectId (pending only), POST /api/decisions (create), PUT /api/decisions/:id (resolve)
- GET filters for pending decisions using `isNull(decisions.resolution)`
- POST broadcasts `decision:created` WebSocket event when a new decision is created
- PUT resolves a decision with chosen option or free-text, broadcasts `decision:resolved` WebSocket event
- `autoDecide` flag prefixes resolution with `[auto-decide]` marker for agent-initiated resolutions
- Guards: 404 for missing decision, 409 for already-resolved decision
- Registered routes in `server/index.ts` via `server.register(decisionRoutes)`
- Files changed: server/routes/decisions.ts (new), server/index.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Decisions schema already existed in `server/db/schema/decisions.ts` — no migration needed
  - `isNull()` from drizzle-orm is used for NULL checks in WHERE clauses
  - `options` column uses `{ mode: "json" }` — drizzle handles JSON serialization/deserialization automatically
  - `resolvedAt` uses `{ mode: "timestamp" }` — pass a JS `Date` object, drizzle stores as unix epoch integer
  - WebSocket broadcast events: `decision:created` and `decision:resolved` — frontend should listen for both
  - Pending decisions = `resolution IS NULL` (not a separate status column)
---

## 2026-03-22 - US-012
- Created `src/api/pipelines.ts` with `Pipeline` and `PipelineStage` interfaces, `usePipelines(projectId)` and `usePipelineStages(pipelineId)` hooks
- Rewrote `PipelineView.tsx` to fetch real data via API hooks instead of hardcoded `PIPELINE_STAGES` array
- Added loading state (spinner), error state, no-project, and no-pipeline empty states
- Added `error` and `skipped` status styling (color, icon, badge) that was missing from the hardcoded version
- Extracted `StageRow` component for cleaner rendering
- Removed hardcoded `PIPELINE_STAGES` array entirely
- Files changed: src/api/pipelines.ts (new), src/components/views/PipelineView.tsx
- **Learnings for future iterations:**
  - Pipeline stages DB schema has `agentId` (FK to agents table) not an agent name — UI shows raw agentId or fallback
  - DB schema has `description` where the old hardcoded UI used `note` — map accordingly
  - `usePipelines` returns all pipelines for a project — PipelineView uses the first one (`pipelines?.[0]`)
  - `useQuery` `enabled` option prevents fetching when params are null — chain with `??` null for conditional queries
  - `SkipForward` from lucide-react works well as the skipped stage icon
  - Manual browser verification needed — no dev-browser tools configured in this session
---

## 2026-03-22 - US-013
- Added `PipelineToolbar` component with Pause, Resume, Cancel buttons and pipeline status badge
- Added `usePausePipeline`, `useResumePipeline`, `useCancelPipeline` mutation hooks to `src/api/pipelines.ts`
- Toolbar shows above pipeline stages with conditionally enabled buttons based on pipeline status
- Pause enabled only when running, Resume only when paused, Cancel when running or paused
- All buttons disabled while any mutation is pending (prevents double-clicks)
- Mutations invalidate the `["pipelines", projectId]` query on success to refresh status
- Status badge shows pipeline state with matching color scheme (idle/running/completed/error/paused/cancelled)
- Files changed: src/api/pipelines.ts, src/components/views/PipelineView.tsx
- **Learnings for future iterations:**
  - Mutation hooks take `projectId` param to invalidate the correct query key on success
  - `isPending` from useMutation is the v5 replacement for `isLoading` — use it to disable buttons during in-flight requests
  - WebSocket-driven real-time updates are deferred to US-016 — for now, query invalidation on mutation success refreshes the UI
  - `Pipeline` type was already exported from `src/api/pipelines.ts` — import it with `import type` for the toolbar props
  - Toolbar uses `activeProjectId!` non-null assertion because it's only rendered when `activePipeline` exists, which requires a non-null projectId
  - Manual browser verification needed — no dev-browser tools configured in this session
---

## 2026-03-22 - US-014
- Created `src/api/decisions.ts` with `Decision` interface, `useDecisions(projectId)` query hook, and `useResolveDecision(projectId)` mutation hook
- Rewrote decision queue section of `RightSidebar.tsx` to fetch real data via API hooks instead of hardcoded content
- Extracted `DecisionCard` component for rendering individual decisions with option buttons and "Let agent decide"
- Option buttons call `PUT /api/decisions/:id` with the selected option text as resolution
- "Let agent decide" button calls the same endpoint with `autoDecide: true` flag
- Added loading, error, and empty states for the decision queue
- All buttons disabled while mutation is pending to prevent double-clicks
- Mutation success invalidates `["decisions", projectId]` query to refresh the list
- Files changed: src/api/decisions.ts (new), src/components/shell/RightSidebar.tsx
- **Learnings for future iterations:**
  - Decision `options` column is JSON but typed as `string[] | null` on the frontend — cast with `as string[] | null`
  - "Let agent decide" uses the first option as fallback resolution text, prefixed with `[auto-decide]` by the server
  - `DecisionCard` is a separate component (not inline) because each card needs its own `useResolveDecision` mutation state
  - Real-time WebSocket updates for decisions deferred to US-016 — for now, query invalidation on mutation success refreshes the UI
  - Manual browser verification needed — no dev-browser tools configured in this session
---

## 2026-03-22 - US-015
- Created `POST /api/pipelines/:id/steer` endpoint in `server/routes/pipelines.ts`
- Endpoint parses simple commands: `pause`, `resume`, `cancel`, `skip [stage name]`, `add [stage name]`
- Returns `{ action: string, result: string }` response with descriptive feedback
- Skip command uses fuzzy matching (`includes`) on stage names (case-insensitive)
- Add command appends a new stage at the end of the pipeline (max sortOrder + 1)
- Created `useSteerPipeline(projectId)` mutation hook in `src/api/pipelines.ts`
- Mutation invalidates both `pipelines` and `pipelineStages` queries on success (since skip/add modify stages)
- Extracted `SteeringInput` component in `PipelineView.tsx` — wraps input in a `<form>` for Enter-to-submit
- Input clears after submission, disabled while mutation is pending
- Loading spinner shown during pending state
- Files changed: server/routes/pipelines.ts, src/api/pipelines.ts, src/components/views/PipelineView.tsx
- **Learnings for future iterations:**
  - Steer endpoint is a router over the existing engine/DB operations — no new engine methods needed
  - Fuzzy stage matching with `includes()` is good enough for keyword steering; exact match would miss partial names
  - `<form onSubmit>` is better than `onKeyDown` for Enter submission — handles form semantics correctly
  - `useSteerPipeline` invalidates `["pipelineStages"]` without a specific ID — invalidates all stage queries since skip/add can change any
  - Full NLP steering deferred to M2 (AI provider integration) — current implementation is keyword-only
  - Manual browser verification needed — no dev-browser tools configured in this session
---

## 2026-03-22 - US-016
- Created `src/hooks/useWebSocket.ts` — connects to `ws://localhost:4321/ws` with automatic reconnection
- Exponential backoff reconnection: starts at 1s, doubles up to 8s max, resets on successful connection
- Listens for `pipeline:status`, `stage:status`, `decision:created`, `decision:resolved` events
- Each event type invalidates the corresponding TanStack Query cache using partial key matching
- Wired `useWebSocket()` into `App.tsx` so it's always active while the app is mounted
- Uses refs for WebSocket, reconnect timer, and unmount flag to avoid stale closure issues
- Cleanup on unmount: clears reconnect timer and closes WebSocket
- Files changed: src/hooks/useWebSocket.ts (new), src/App.tsx
- **Learnings for future iterations:**
  - `queryClient.invalidateQueries({ queryKey: ["pipelines"] })` invalidates ALL queries whose key starts with `["pipelines"]` — no need to specify the full key
  - WebSocket `onclose` fires after `onerror`, so reconnection logic only needs to live in `onclose`
  - Use `useRef` for mutable state in effects (ws instance, timers, unmounted flag) to avoid dependency array issues
  - Browser WebSocket API uses `onmessage`, `onclose`, `onerror`, `onopen` — not `.on("event")` like Node ws
  - Hook returns void — it's a side-effect-only hook, not a data hook
---
