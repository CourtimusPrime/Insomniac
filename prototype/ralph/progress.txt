# Ralph Progress Log
Started: Sun Mar 22 19:11:30 UTC 2026

## Codebase Patterns
- Server code lives in `server/` and is typechecked with `tsconfig.server.json` (`npx tsc --project tsconfig.server.json --noEmit`)
- Routes are Fastify plugins exported as `async function xyzRoutes(server: FastifyInstance)`
- Routes are registered in `server/index.ts` via `await server.register(xyzRoutes)`
- Use `.js` extensions in imports (ESM module resolution)
- Pre-existing typecheck error in `server/marketplace/client.ts:142` — not introduced by us
- `getRunnerForProject(projectId)` is exported from `server/routes/localhost.ts` to access dev server state from other modules
- Fastify wildcard routes use `server.get<{ Params: { "*": string } }>("/path/*", ...)` — access via `request.params["*"]`
- Frontend API hooks live in `src/api/*.ts` — use `apiFetch` from `./client`, TanStack Query `useQuery`/`useMutation`, and `queryClient.invalidateQueries` on success
- WebSocket broadcast pattern: import `broadcast` from `server/ws/handler.js`, call `broadcast(eventName, data)` to push events to all connected clients
- Playwright `ConsoleMessage.type()` returns `'warning'` not `'warn'` — map accordingly when normalizing levels
---

## 2026-03-22 - US-001
- Created `server/browser/types.ts` with `BrowserEngine` interface and `BrowserConfig` type
- Created `server/browser/index.ts` re-exporting types
- Files changed: `server/browser/types.ts`, `server/browser/index.ts`
- **Learnings for future iterations:**
  - The `server/` directory has its own tsconfig (`tsconfig.server.json`) separate from the frontend
  - There's a pre-existing unused variable error in marketplace code — don't try to fix it
  - Abilities live in `server/abilities/` — this is where browser tools will go (US-003)
---

## 2026-03-22 - US-002
- Created `server/browser/playwright-adapter.ts` implementing `BrowserEngine` with Playwright's Chromium
- All interface methods (launch, navigate, click, fill, screenshot, getText, evaluate, close) delegate to Playwright's Page API
- `screenshot()` returns base64 PNG string via `buffer.toString("base64")`
- Added `playwright` to package.json dependencies
- Exported `PlaywrightAdapter` from `server/browser/index.ts`
- Files changed: `server/browser/playwright-adapter.ts`, `server/browser/index.ts`, `package.json`, `package-lock.json`
- **Learnings for future iterations:**
  - Playwright requires `npx playwright install chromium` to download browser binaries before first use
  - The adapter holds a single Page instance — future stories may need multi-page support
  - `evaluate()` accepts a string script (not a function) per the BrowserEngine interface
---

## 2026-03-22 - US-003
- Created `server/browser/browser-tools.ts` with 9 MCP-compatible tool definitions
- Tools: navigate, click, fill, screenshot, assertText, assertElement, getConsoleErrors, evaluateScript, waitForSelector
- Each tool handler delegates to the `BrowserEngine` abstraction
- `executeBrowserTool()` function dispatches tool calls by name
- `browserTestAbility` defines the 'browser-test' Ability record (type: "plugin")
- Exported all public types and functions from `server/browser/index.ts`
- Files changed: `server/browser/browser-tools.ts`, `server/browser/index.ts`
- **Learnings for future iterations:**
  - Abilities use type `"plugin"` for internal tool-providing abilities (vs `"skill"` for SKILL.md imports and `"mcp"` for external MCP servers)
  - `getConsoleErrors` relies on `__insomniac_console_errors` global — a console.error override must be injected after page navigation for this to work
  - `waitForSelector` uses an in-page polling evaluate script since BrowserEngine doesn't expose native waitFor
  - Tool input schemas follow MCP JSON Schema format: `{ type: "object", properties, required }`
---

## 2026-03-22 - US-004
- Created `server/routes/browser.ts` as a Fastify plugin with 6 endpoints
- Endpoints: POST launch, POST navigate, POST screenshot, POST evaluate, POST close, GET status
- Singleton `BrowserEngine` instance shared across routes
- Input validation via Fastify JSON Schema on navigate (url) and evaluate (script) POST bodies
- All mutation endpoints check browser state and return 400 if preconditions not met
- Registered `browserRoutes` in `server/index.ts`
- Files changed: `server/routes/browser.ts`, `server/index.ts`
- **Learnings for future iterations:**
  - Browser routes use a module-level singleton engine (not per-request) — same pattern as `runners` Map in localhost.ts
  - Fastify input validation uses JSON Schema in the `schema.body` option (see settings.ts for examples with pattern validation)
  - The launch endpoint hardcodes `headless: true` — future stories may want to make this configurable
  - US-005 (proxy endpoint) will need to be added to this same routes file or a separate one
---

## 2026-03-22 - US-005
- Added `GET /api/browser/proxy/*` route to `server/routes/browser.ts`
- Proxy requires `?projectId=xxx` query parameter to identify which dev server to proxy to
- Uses `getRunnerForProject()` exported from `localhost.ts` to discover the dev server port
- Proxies requests via Node's native `http.request` to `http://127.0.0.1:{port}/{path}`
- Streams response back including content-type, content-length, and cache-control headers
- Returns 502 if dev server is not running or not reachable
- Adds CORS headers (Access-Control-Allow-Origin for localhost:5173)
- Files changed: `server/routes/browser.ts`, `server/routes/localhost.ts`
- **Learnings for future iterations:**
  - Fastify wildcard routes: use `{ Params: { "*": string } }` generic and `request.params["*"]` to access the captured path
  - Fastify querystring validation uses `schema.querystring` (not `schema.query`)
  - Node's `http.request` callback receives an `IncomingMessage` (readable stream) — Fastify's `reply.send()` accepts streams directly
  - Avoid unused parameters in Promise constructors — TypeScript's `noUnusedParameters` will catch them
  - The proxy streams responses (no buffering), making it suitable for HTML, CSS, JS, and assets
---

## 2026-03-22 - US-006
- Created `src/api/browser.ts` with 5 TanStack Query hooks: `useBrowserStatus()`, `useLaunchBrowser()`, `useNavigate()`, `useScreenshot()`, `useCloseBrowser()`
- `useBrowserStatus` polls every 5s via `refetchInterval`, matching the pattern in `localhost.ts`
- All mutations invalidate `browser-status` query key on success
- `useScreenshot` returns `ScreenshotResult` with `image` (base64 string)
- Files changed: `src/api/browser.ts`
- **Learnings for future iterations:**
  - Frontend API hooks live in `src/api/` as plain `.ts` files (not `.tsx`) — they only use hooks, no JSX
  - Follow the pattern in `src/api/localhost.ts` for mutations: `useMutation` + `queryClient.invalidateQueries` on success
  - `apiFetch` already sets `Content-Type: application/json` header, so mutations just need `body: JSON.stringify(params)`
  - The `browser-status` query key is the central invalidation target for all browser mutations
---

## 2026-03-22 - US-007
- Replaced placeholder Browser tab in BottomPanel with real browser integration
- Shows iframe pointing to proxy endpoint (`/api/browser/proxy/?projectId=...`) when dev server is running
- URL bar with form submission for navigation via `useNavigate` hook
- Refresh button increments iframe key to force reload
- Launch/Close browser buttons toggle Playwright engine via `useLaunchBrowser`/`useCloseBrowser`
- "Dev server not running" state with Start button when no server active
- "Select a project" state when no project selected
- Browser status indicator (green/gray dot) in toolbar
- Files changed: `src/components/shell/BottomPanel.tsx`
- **Learnings for future iterations:**
  - The iframe uses `sandbox="allow-scripts allow-same-origin allow-forms allow-popups"` for security isolation
  - Incrementing an iframe `key` prop is the React-idiomatic way to force a full iframe reload
  - The browser tab reuses `devStatus` and `startServer` from the health tab — they're already declared at component top level
  - Use `min-h-0` on flex children with `flex-1` to allow them to shrink properly in nested flex layouts
  - The proxy endpoint requires `?projectId=` query param to identify which dev server to proxy to
---

## 2026-03-22 - US-008
- Added Screenshot button to Browser tab toolbar (Camera icon, accent-primary styled)
- Button triggers `useScreenshot` mutation; on success shows screenshot in preview overlay
- Overlay appears over the iframe with Copy, Save, and Close buttons
- Copy to clipboard uses `navigator.clipboard.write` with `ClipboardItem` for image/png
- Save downloads as PNG via a temporary anchor element with `data:` URI
- Screenshot preview state (`screenshotPreview`) holds the base64 string, null when closed
- Files changed: `src/components/shell/BottomPanel.tsx`
- **Learnings for future iterations:**
  - `navigator.clipboard.write` with `ClipboardItem({ 'image/png': blob })` is the way to copy images to clipboard in modern browsers
  - Converting base64 to blob: `fetch('data:image/png;base64,...').then(r => r.blob())` is clean and avoids manual ArrayBuffer conversion
  - Overlay pattern: use `absolute inset-0` on a parent with `relative` class to create a full-coverage overlay within a container
  - The screenshot preview uses the iframe container as its positioning context (added `relative` to the iframe wrapper div)
---

## 2026-03-22 - US-009
- Added Console sub-panel below the browser preview in the Browser tab
- Server-side: Added `ConsoleEntry` type and `onConsole(callback)` to `BrowserEngine` interface
- PlaywrightAdapter captures Playwright `page.on('console')` events and forwards them via callback
- Browser routes wire up `onConsole` to `broadcast('browser:console', entry)` via WebSocket
- Frontend: WebSocket listener for `browser:console` events populates console entries state
- Each entry shows: level icon (Info/AlertTriangle/AlertCircle), timestamp, message text
- Color-coded: info=text-muted, warn=amber-400, error=status-error
- Clear button (Trash2 icon) resets the console log
- Layout splits iframe (flex-[2]) and console (flex-1) vertically within the browser tab
- Files changed: `server/browser/types.ts`, `server/browser/playwright-adapter.ts`, `server/browser/index.ts`, `server/routes/browser.ts`, `src/components/shell/BottomPanel.tsx`
- **Learnings for future iterations:**
  - Playwright `ConsoleMessage.type()` returns `'warning'` (not `'warn'`) — must map to our `'warn'` level
  - Register `onConsole` callback BEFORE `launch()` to avoid missing early console messages during page load
  - Use `flex-[2]` vs `flex-1` to create a 2:1 split in flex containers without hardcoded heights
  - The BottomPanel Browser tab now opens a second WebSocket for `browser:console` events, separate from the `devserver:log` listener
  - Browser console state is not project-scoped (unlike dev server logs) since there's only one browser singleton
- Creating a pipeline requires `workspaceId` — look it up from the `projects` table via the `projectId`
---

## 2026-03-22 - US-010
- Added agent activity broadcasting to `executeBrowserTool` in `server/browser/browser-tools.ts`
- Each tool execution broadcasts `browser:agent-action` WebSocket events with pending → done/error lifecycle
- Actions have unique IDs, human-readable descriptions (e.g., "clicking #submit-btn"), and status
- Frontend: Added Agent Activity sub-panel side-by-side with Console in the Browser tab
- Status icons: Loader2 (spinning) for pending, CheckCircle for done, XCircle for error
- Entries update in-place when status changes from pending → done/error (matched by action ID)
- Auto-scrolls to latest entry
- Clear button to reset activity feed
- Files changed: `server/browser/browser-tools.ts`, `src/components/shell/BottomPanel.tsx`
- **Learnings for future iterations:**
  - Consolidated `browser:console` and `browser:agent-action` WebSocket listeners into one connection to avoid excessive WS connections (BottomPanel already had 2 separate WS connections)
  - For state updates that modify existing entries by ID, use `findIndex` + spread to update in-place rather than just appending
  - Side-by-side layout (flex-row) for sub-panels is more space-efficient than stacking vertically when the bottom panel has limited height
  - The `describeAction` helper maps tool names to human-readable strings — keep this in sync when adding new browser tools
---

## 2026-03-22 - US-011
- Added `POST /api/browser/inspect-in-agent` endpoint to `server/routes/browser.ts`
- Endpoint accepts `{ selector, description, projectId }`, looks up the project's workspaceId, creates a pipeline with a single stage whose description is `Fix this element: [selector] — [description]`
- Returns `{ stageId, pipelineId }`
- Added `useInspectInAgent` hook to `src/api/browser.ts`
- Added "Inspect" button (Crosshair icon) to Browser tab toolbar
- Clicking opens an overlay dialog with selector and description inputs
- Submit sends the request; on success shows a confirmation message that auto-dismisses after 2 seconds
- Files changed: `server/routes/browser.ts`, `src/api/browser.ts`, `src/components/shell/BottomPanel.tsx`
- **Learnings for future iterations:**
  - Pipeline creation requires a `workspaceId` — look it up from the project record via `db.select().from(projects).where(eq(projects.id, projectId)).get()`
  - Import `pipelines` and `pipelineStages` from `server/db/schema/index.js` (not individual files) for consistency
  - The overlay dialog pattern reuses the same `absolute inset-0 z-10` approach as the screenshot preview
  - For temporary confirmation UIs, `setTimeout` to auto-dismiss works well — just remember to clear both the dialog state and the confirmation state
---
