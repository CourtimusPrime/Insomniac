# Ralph Progress Log
Started: Sun Mar 22 18:31:48 UTC 2026
---

## Codebase Patterns
- Schema files use `sqliteTable` from drizzle-orm/sqlite-core with `.js` extension in imports
- Frontend API hooks pattern: `useQuery` for GET, `useMutation` with `useQueryClient().invalidateQueries` for mutations
- API hooks import `apiFetch` from `./client` (base URL http://localhost:4321)
- Query keys follow `["resource"]` for lists, `["resource", id]` for single items
- Mutations invalidate all related query keys on success (e.g., install invalidates both marketplace + templates)
- Primary keys use `text("id").$defaultFn(() => crypto.randomUUID())`
- Timestamps use `integer("created_at", { mode: "timestamp" }).default(sql\`(unixepoch())\`)`
- Booleans use `integer("col", { mode: "boolean" })`
- JSON columns use `text("col", { mode: "json" })`
- Barrel exports in schema/index.ts use `.js` extensions
- Migration workflow: `npx drizzle-kit generate` then `npx drizzle-kit migrate`
- Typecheck: `npx tsc --noEmit`
- Fastify routes are registered as plugins in server/index.ts
- Seed pattern: define data array + `seedIfEmpty()` function that checks `.all().length > 0`, called at route plugin registration time
- Route plugins call seed functions at top of the exported function (e.g., `await seedBuiltInTemplates()`)
- Apply template pattern: copy `chainDefinition` from template to project, increment `installCount`
- Marketplace client uses a mock store index (array of MarketplaceItem) — will be replaced by GitHub fetch later
- Marketplace routes use `getOrCreateDefaultWorkspace()` helper (duplicated — consider extracting to shared util)
- Pagination pattern: parse page/limit from querystring, slice results, return `{ items, total, page, limit, totalPages }`
- MarketplaceClient class pattern: constructor takes optional storeUrl, async methods return typed data
- `getOrCreateDefaultWorkspace()` helper returns the default workspace ID (duplicated across routes — consider extracting)
- ChainDefinition format: `{ version: 1, nodes: [{id, type, label, model, systemPrompt, status, abilities, position}], edges: [{id, source, target, condition}] }`
- Node types: trigger, prototyper, builder, tester, reviewer, auditor
- Edge conditions: always, on-success, on-failure, on-decision
- MainView routing: add view name to `MainView` type in stores/layout.ts, import + render in MainView.tsx
- Category tab filter pattern: local state CategoryTab, map over CATEGORIES array, conditionally include in filters object
- Grid card pattern: `grid grid-cols-2 gap-3` with `rounded-lg border border-border-default bg-bg-base p-4`
- Badge color maps: define as `Record<string, string>` constants (TYPE_COLORS, TIER_CONFIG)
- MarketplaceView uses max-w-4xl (wider than other views) since it has a 2-column grid
---

## 2026-03-22 - US-001
- Created `server/db/schema/templates.ts` with 12 columns: id, workspaceId, name, description, category (enum), chainDefinition (JSON), author, version, isBuiltIn (boolean), installCount, createdAt, updatedAt
- Updated `server/db/schema/index.ts` barrel export
- Generated migration `0007_abnormal_callisto.sql` and ran it successfully
- Files changed: server/db/schema/templates.ts, server/db/schema/index.ts, server/db/migrations/0007_abnormal_callisto.sql
- **Learnings for future iterations:**
  - Category enum values: 'workflow', 'agent-config', 'template', 'mcp-adapter'
  - workspaceId references workspaces.id (same FK pattern as all other tables)
  - The drizzle config at drizzle.config.ts points to `./data/insomniac.db` for SQLite
---

## 2026-03-22 - US-002
- Created `server/templates/built-in.ts` with 6 built-in pipeline template definitions
- Templates: Full Stack Build, Security Audit, Code Review, Rapid Prototype, Test Suite, Full Pipeline
- Each template has a complete chainDefinition with nodes and edges using `buildChain()` helper
- Exported `seedBuiltInTemplates()` function that inserts templates on first run if table is empty
- Files changed: server/templates/built-in.ts
- **Learnings for future iterations:**
  - `buildChain()` helper generates linear chains from a list of node types with consistent positioning (x+280 per node, y=180)
  - The seed function follows the same pattern as projects/providers: check `.all().length > 0`, skip if data exists
  - `seedBuiltInTemplates()` needs to be called from somewhere at startup (e.g., from the templates route plugin when it's created in US-003)
  - Template `chainDefinition` is stored as JSON in a `text` column with `{ mode: "json" }`
---

## 2026-03-22 - US-003
- Created `server/routes/templates.ts` as a Fastify plugin with 5 endpoints:
  - GET /api/templates — list all templates
  - GET /api/templates/:id — get single template
  - POST /api/templates — create user template with JSON Schema validation
  - POST /api/templates/:id/apply — apply template to project (copies chainDefinition, increments installCount)
  - DELETE /api/templates/:id — delete user template (403 for built-in)
- Calls `seedBuiltInTemplates()` at plugin registration time
- Registered routes in server/index.ts
- Files changed: server/routes/templates.ts (new), server/index.ts
- **Learnings for future iterations:**
  - Apply endpoint needs both template ID (from params) and projectId (from body)
  - Built-in templates are protected from deletion with a 403 response
  - Fastify generic types: `<{ Params: {...}; Body: {...} }>` for combined param + body routes
  - POST schema validation uses `additionalProperties: false` to reject unexpected fields
---

## 2026-03-22 - US-004
- Created `server/marketplace/types.ts` with MarketplaceItem, MarketplaceItemType, TrustTier, and MarketplaceFilters types
- Created `server/marketplace/client.ts` with MarketplaceClient class and 8 mock marketplace items
- MarketplaceClient methods: fetchItems(filters?), getItem(id), downloadItem(id)
- Mock store index covers all 4 item types (workflow, agent-config, template, mcp-adapter) and all 3 trust tiers (community, verified, official)
- Files changed: server/marketplace/types.ts (new), server/marketplace/client.ts (new)
- **Learnings for future iterations:**
  - MarketplaceClient is stateless — instantiate with optional storeUrl, defaults to insomniac-dev/store GitHub raw URL
  - fetchItems supports filtering by type, trustTier, and search (name + description match)
  - downloadItem returns a generic Record<string, unknown> since different item types have different payloads
  - The mock index has 8 items which is enough to test pagination, filtering, and search in the marketplace UI
  - When switching to real GitHub fetch, replace the MOCK_STORE_INDEX usage in each method with a fetch() call
---

## 2026-03-22 - US-005
- Created `server/routes/marketplace.ts` as a Fastify plugin with 3 endpoints:
  - GET /api/marketplace — paginated list with optional filters (type, trustTier, search, page, limit)
  - GET /api/marketplace/:id — single item detail
  - POST /api/marketplace/:id/install — downloads item payload, creates a template in local DB
- Input validation via JSON Schema on querystring (enum constraints) and body
- Pagination: page/limit params, returns `{ items, total, page, limit, totalPages }`
- Install creates a template record from the marketplace item's chainDefinition
- Registered routes in server/index.ts
- Files changed: server/routes/marketplace.ts (new), server/index.ts
- **Learnings for future iterations:**
  - `getOrCreateDefaultWorkspace()` is now duplicated in 3 files — should be extracted to a shared util
  - Querystring params come as strings in Fastify, need parseInt for page/limit
  - Install endpoint uses optional workspaceId from body, falls back to default workspace
  - The marketplace client's `downloadItem()` returns a `Record<string, unknown>` — cast `payload.chainDefinition` carefully
  - Pagination limits clamped: page >= 1, 1 <= limit <= 50
---

## 2026-03-22 - US-006
- Created `src/api/templates.ts` with 5 hooks: useTemplates(), useTemplate(id), useApplyTemplate(), useCreateTemplate(), useDeleteTemplate()
- Created `src/api/marketplace.ts` with 3 hooks: useMarketplace(filters), useMarketplaceItem(id), useInstallItem()
- All mutations invalidate relevant query keys (templates, marketplace, projects)
- Marketplace hook builds URLSearchParams from filters object for query string
- Files changed: src/api/templates.ts (new), src/api/marketplace.ts (new)
- **Learnings for future iterations:**
  - Template type mirrors DB schema but uses `string` for timestamps (JSON serialization)
  - MarketplaceFilters includes pagination (page/limit) since the server paginates
  - useMarketplace uses the full filters object as the query key for automatic refetch on filter changes
  - useInstallItem invalidates both `["marketplace"]` and `["templates"]` since installing creates a template
  - The marketplace.ts imports Template type from templates.ts for the install response typing
---

## 2026-03-22 - US-007
- Created `src/components/views/MarketplaceView.tsx` with grid layout, search, category filters, and item cards
- Added 'marketplace' to MainView type in `src/stores/layout.ts`
- Added MarketplaceView route in `src/components/shell/MainView.tsx`
- Each card shows: name, type badge (color-coded), trust tier icon, author, install count, description preview
- Category filter tabs: All, Workflows, Agent Configs, Templates, MCP Adapters
- Search input filters by name/description via the marketplace API
- Files changed: src/components/views/MarketplaceView.tsx (new), src/components/shell/MainView.tsx, src/stores/layout.ts
- **Learnings for future iterations:**
  - Adding a new view: (1) add to MainView type in layout.ts, (2) import + render conditionally in MainView.tsx
  - useMarketplace hook accepts optional filters — pass `undefined` when no filters active (avoids empty object query key issues)
  - `line-clamp-2` Tailwind utility works for truncating multi-line text
  - Trust tier badges use different lucide icons: Shield (community), ShieldCheck (verified), ShieldAlert (official)
  - MarketplaceView uses max-w-4xl to accommodate the 2-column grid layout
  - Browser verification needed — no dev-browser available in this session
---
